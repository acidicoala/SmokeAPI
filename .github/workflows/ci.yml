name: Continuous Integration
on: push

jobs:
  ci:
    name: CI
    uses: acidicoala/KoalaBox/.github/workflows/build-and-package.yml@0544941fb33db220f15eab2644713f0b034996cf
    permissions:
      contents: write
    with:
      arch: '[ 32, 64 ]'
      config: Release
      modules: '[ "SmokeAPI" ]'
      zip_command: >
        zip -j $ZIP_NAME
        artifacts/*/*.dll
        res/SmokeAPI.config.json
        res/README.txt
