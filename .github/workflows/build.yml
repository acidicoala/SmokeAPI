name: 🏗️ Build
on: push

jobs:
  build:
    name: 🏗️ Build
    uses: ./kb-build.yml
    with:
      #language=json
      module: '[ "SmokeAPI" ] '

      #language=json
      os: '[ "Windows", "Linux" ]'

      #language=json
      bitness: '[ 32, 64 ]'
